{
  "name": "mast",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "overrides": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "scripts": {
    "test": "npm test --workspace=packages/orchestrator && npm test --workspace=packages/daemon"
  }
}
