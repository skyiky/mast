{
  "name": "mast",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "overrides": {
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-native-worklets": "0.5.1"
  },
  "scripts": {
    "test": "npm test --workspace=packages/orchestrator && npm test --workspace=packages/daemon && npm test --workspace=packages/mobile",
    "lint": "npm run lint:selectors --workspace=packages/mobile",
    "pretest": "npm run lint"
  }
}
