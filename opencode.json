{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "rc": {
      "template": "The user executed the /rc system command. This is not a question or request — do not take any action. Reply with only: Done.",
      "description": "Toggle Mast remote control"
    }
  }
}
